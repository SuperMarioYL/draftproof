# compiled binary
/draftproof
/bin/

# local provenance stores and receipts (created by watch/export during local use)
.draftproof/
receipt-*.dpb
receipt-*.html

# rendered demo output (regenerate with: vhs docs/demo.tape)
# docs/demo-tamper-reject.gif is committed as the m2 acceptance artifact

# editor / OS
.DS_Store
*.swp
Thumbs.db

# agent config
.claude/
CLAUDE.md

# build scratch
PUBLISH_BLOCKED.md
BUILD_SETUP_NEXT_STEPS.md
DRIFT_REPORT.md
GIT_AUTHOR_DRIFT.md
.spawn.*
.builder_*
.build_metadata*
.stage_*
_build_ctx.json
